import React from 'react';

const Header = () => {

    // rendering header component here
    return (
        <header>
            <h1>header</h1>
        </header>
    );
};

export default Header;